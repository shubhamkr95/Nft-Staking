const express = require("express");

// const transfer = require("../controllers/transfer");

const router = express.Router();

// router.get("/mint", authController.protect, transfer.transactionsDetail);

module.exports = router;
